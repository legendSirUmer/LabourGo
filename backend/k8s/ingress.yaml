apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: labourgo-ingress

spec:
  ingressClassName: nginx

  rules:
  - host: labourgo.local

    http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: django-service

            port:
              number: 80